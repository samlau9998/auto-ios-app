import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            List {
                // List of passages
            }
            .navigationTitle("Reading Passages")
        }
    }
}